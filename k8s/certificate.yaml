apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: kenshin-wildcard
spec:
  secretName: kenshin-wildcard-tls
  issuerRef:
    name: kenshin-ca
    kind: ClusterIssuer
  dnsNames:
    - "*.kenshin.local"
    - "kenshin.local"
